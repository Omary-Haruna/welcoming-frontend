import React from "react";

const OrderStatisticsPage = () => {
    return (
        <div>
            <h1>Order Statistics</h1>
            <p>This page will show order statistics like most ordered items, total orders, etc.</p>
        </div>
    );
};

export default OrderStatisticsPage;
